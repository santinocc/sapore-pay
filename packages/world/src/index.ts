/**
 * World — implemented during the hackathon.
 * Planned: HumanVerifier via World ID cloud verification with IDKit
 * (POST /api/v2/verify/{app_id}), action `chef-onboarding`, storing only
 * the nullifier hash per action -- never the proof.
 */
export {}
