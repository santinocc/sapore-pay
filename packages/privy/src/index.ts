/**
 * Privy glue — implemented during the hackathon.
 * Planned: verify Sapore RS256 access tokens against its JWKS (custom auth),
 * server-wallet signer for the escrow/treasury with destination allowlists.
 */
export {}
